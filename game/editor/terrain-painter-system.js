import { default as Core } from '@core/core';
import { default as Entity } from '@core/entity.js';
import { default as System } from '@core/system';

export default class TerrainPainterSystem extends System {
    constructor() {
        super()
    }

    initialize() {
        // this._core.publishData('EDITOR_SELECTED_TERRAIN', )
    }
}

