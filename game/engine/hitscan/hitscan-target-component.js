import { default as Component} from '@core/component'

export default class HitscanTargetComponent extends Component {
    constructor(payload = {}) {
        super();
        this.componentType = "HitscanTargetComponent"
    }
}