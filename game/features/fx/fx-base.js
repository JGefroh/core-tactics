class FxBase {
    static getFxKey() {
        return this.name;
    }

    preExecute(core, params) {
    }

    execute(core, params) {
    }

    postExecute(core, params) {
    }

    undo(core, params) {
    }
}

export default FxBase;