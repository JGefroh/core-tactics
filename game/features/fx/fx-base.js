class FxBase {
    static getFxKey() {
    }

    preExecute(core, params) {
    }

    execute(core, params) {
    }

    postExecute(core, params) {
    }

    undo(core, params) {
    }
}

export default FxBase;