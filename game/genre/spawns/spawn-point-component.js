import { default as Component } from '@core/component';

export default class SpawnPointComponent extends Component {
    constructor(payload = {}) {
        super();
        this.componentType = 'SpawnPointComponent';
    }
} 